# packages/shared

TypeScript types, enums, and constants shared between the API and all frontends (plan tiers, swipe types, report categories, notification types, API DTO shapes). Never duplicate these in apps.
